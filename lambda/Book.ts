class Book {
    isbn: number;
    name: string;
    author: string;
    
    constructor(isbn: number, name: string, author: string) {
        this.isbn = isbn;
        this.name = name;
        this.author = author;
    }
}